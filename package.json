{
  "name": "cmless",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "bun generate && astro dev",
    "build": "bun generate && astro build && bun run pagefind",
    "pagefind": "rm -rf dist/pagefind public/pagefind && pagefind --site dist && cp -r dist/pagefind public/pagefind",
    "generate": "bun scripts/generate.ts",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/rss": "^4.0.19",
    "astro": "^7.1.3",
    "pagefind": "^1.5.2"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.20",
    "@tailwindcss/vite": "^4.3.3",
    "tailwindcss": "^4.3.3"
  }
}
