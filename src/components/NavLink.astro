---
const { class: className, ...props } = Astro.props as { class?: string; [key: string]: unknown };
---

<a class={`text-slate-500 dark:text-slate-400 no-underline hover:text-blue-700 dark:hover:text-blue-400 ${className}`} {...props}>
  <slot />
</a>
